{
  "name": "video-downloader",
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "start": "apt update && apt install -y python3-pip && pip3 install yt-dlp && npm run bot",
    "bot": "node --loader ts-node/esm src/bot.ts"
  },
  "dependencies": {
    "dotenv": "^16.0.3",
    "telegraf": "^4.12.2"
  },
  "devDependencies": {
    "@types/node": "^18.15.11",
    "ts-node": "^10.9.2",
    "typescript": "^5.0.0"
  }
}
